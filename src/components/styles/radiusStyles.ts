import { Radius } from "../types/Radius";

export const radiusStyles: Record<Radius,number> = {
    none: 0,
    xs: 10,
    sm: 12,
    md: 14,
    lg: 16,
    xl: 18,
    '2xl': 20,
    full: 100
};
